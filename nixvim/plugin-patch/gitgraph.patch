--- a/lua/gitgraph/core.lua
+++ b/lua/gitgraph/core.lua
@@ -647,7 +647,7 @@ function M._gitgraph(raw_commits, opt, sym, fields)
           local pad_str = (' '):rep(pad_size)
           add_to_row(pad_str)
           if is_head then
-            add_to_row('*')
+            add_to_row('✔')
           end
 
           --- add hihlights for hash, timestamp, branch_names and tags
